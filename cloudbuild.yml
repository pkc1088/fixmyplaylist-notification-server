steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - --destination=$_IMAGE_NAME
      - --cache=true
      - --cache-ttl=336h
      - --compressed-caching=false

timeout: 1200s

substitutions:
  _IMAGE_NAME: ''